# The dashboard lives under /app and is a single page; Cloudflare Pages must
# serve index.html for it rather than a 404. The landing page is the root.
/app       /index.html   200
/app/*     /index.html   200
